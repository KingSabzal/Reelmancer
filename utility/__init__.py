"""Engine packages: everything the application does that is not the interface.

Grouped here so the project root stays readable. Each subpackage owns one stage
of the pipeline, from configuration and language models through to rendering and
the publishing package.
"""
